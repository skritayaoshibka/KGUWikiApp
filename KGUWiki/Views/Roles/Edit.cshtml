@model KGUWiki.Models.Role
@{
    ViewBag.Title = "Редактирование записи в таблице пользовательских ролей";
}

<a class="btn btn-outline-secondary m-2" asp-action="Index" asp-controller="Roles">🡸Вернуться к таблице</a>
<div class="mx-auto w-50 m-3">
    <h2 class="text-center mb-3">Редактирование информации о пользовательской роли</h2>
    <hr />
    <form asp-action="Edit">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <span asp-validation-for="Name" class="text-danger"></span>
        <div class="input-group mb-3">
            <div class="input-group-prepend">
                <span class="input-group-text">@Html.DisplayNameFor(model => model.Name)</span>
            </div>
            <input asp-for="Name" class="form-control" value="@Model.Name" />
        </div>

        <div class="form-group">
            <input type="submit" value="Изменить" class="btn btn-success" />
        </div>
    </form>
</div>

@section Scripts {
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}